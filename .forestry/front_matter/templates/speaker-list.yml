---
label: speaker-list
hide_body: true
fields:
- name: speakers
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: pages
      section: speakers
  label: speakers
pages:
- _data/speaker-list.yml
