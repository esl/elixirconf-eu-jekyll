---
label: speaker-list
hide_body: true
fields:
- name: speakers
  type: field_group_list
  fields:
  - name: speaker
    type: select
    default: []
    config:
      required: true
      options: []
      source:
        type: pages
        section: speakers
        file: 
        path: 
    label: speaker
  config:
    min: 
    max: 
    labelField: speaker
  label: speakers
pages:
- _data/speaker-list.yml
