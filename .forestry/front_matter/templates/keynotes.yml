---
label: keynote-list
hide_body: true
fields:
- name: keynotes
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: pages
      section: participants
  label: keynotes
pages:
- _data/keynote-list.yml
